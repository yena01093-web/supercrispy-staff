{
  "firestore": {
    "rules": "firestore.rules"
  },
  "hosting": {
    "public": ".",
    "ignore": ["firebase.json", "firestore.rules", ".firebaserc", "**/.*"],
    "rewrites": [
      { "source": "**", "destination": "/index.html" }
    ]
  }
}
